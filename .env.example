# MongoDB connection string (required at runtime)
# Atlas example: mongodb+srv://user:password@cluster.mongodb.net/video-platform
MONGODB_URI=mongodb://127.0.0.1:27017/video-platform

# Optional fallback (also supported by src/db/index.ts)
# DATABASE_URL=
